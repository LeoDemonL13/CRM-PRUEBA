(function(){
'use strict';
const $=id=>document.getElementById(id);
const text=value=>String(value??'').trim();
const esc=value=>text(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
const fmtDate=value=>{if(!value)return'—';const d=new Date(`${value}T00:00:00`);return Number.isNaN(d.getTime())?'—':d.toLocaleDateString('es-MX',{day:'2-digit',month:'short',year:'numeric'})};
const daysUntil=value=>{if(!value)return null;const now=new Date();now.setHours(0,0,0,0);const d=new Date(`${value}T00:00:00`);if(Number.isNaN(d.getTime()))return null;return Math.ceil((d-now)/86400000)};
let vehicles=[];
let loadPromise=null;
function docState(vehicle){const dates=[['Seguro',vehicle.vigenciaSeguro],['Tarjeta',vehicle.vigenciaTarjeta],['Verificación',vehicle.proximaVerificacion]].filter(item=>item[1]);if(!dates.length)return{tone:'muted',label:'Sin vigencias registradas'};let nearest=null;for(const [name,date] of dates){const days=daysUntil(date);if(days===null)continue;if(!nearest||days<nearest.days)nearest={name,date,days}}if(!nearest)return{tone:'muted',label:'Sin vigencias registradas'};if(nearest.days<0)return{tone:'danger',label:`${nearest.name} vencido hace ${Math.abs(nearest.days)} días`};if(nearest.days<=30)return{tone:'warning',label:`${nearest.name} vence en ${nearest.days} días`};return{tone:'ok',label:`${nearest.name}: ${fmtDate(nearest.date)}`}}
function render(){const query=text($('search')?.value);const status=text($('status')?.value).toLowerCase();const filtered=vehicles.filter(v=>(!query||(window.SkilledSearch?.matches?window.SkilledSearch.matches([v.numeroEconomico,v.placas,v.marca,v.modelo,v.anio,v.tipo,v.proyecto,v.responsable,v.asignadoA,v.vin,v.combustible,v.estado],query):[v.numeroEconomico,v.placas,v.marca,v.modelo,v.tipo,v.proyecto,v.responsable,v.asignadoA].join(' ').toLowerCase().includes(query.toLowerCase())))&&(!status||text(v.estado).toLowerCase()===status));$('count').textContent=filtered.length.toLocaleString('es-MX');$('available').textContent=vehicles.filter(v=>text(v.estado).toLowerCase()==='disponible').length.toLocaleString('es-MX');$('assigned').textContent=vehicles.filter(v=>['asignado','en_uso','en uso'].includes(text(v.estado).toLowerCase())||text(v.proyecto)).length.toLocaleString('es-MX');$('docs').textContent=vehicles.filter(v=>{const s=docState(v);return s.tone==='danger'||s.tone==='warning'}).length.toLocaleString('es-MX');const grid=$('grid');if(!filtered.length){grid.innerHTML='<div class="dir-vehicle-empty">No hay vehículos que coincidan con los filtros.</div>';return}grid.innerHTML=filtered.map(v=>{const docs=docState(v);const state=text(v.estado)||'disponible';const image=text(v.imagen);return `<article class="dir-vehicle-card"><div class="dir-vehicle-top"><div class="dir-vehicle-photo">${image?`<img src="${esc(image)}" alt="${esc(v.numeroEconomico)}" loading="lazy">`:'<span>V</span>'}</div><div class="dir-vehicle-name"><span>${esc(v.tipo||'Vehículo')}</span><h3>${esc(v.numeroEconomico||'Sin nombre')}</h3><p>${esc([v.marca,v.modelo,v.anio].filter(Boolean).join(' · ')||'Sin ficha técnica')}</p></div><span class="dir-state" data-state="${esc(state.toLowerCase().replace(/\s+/g,'-'))}">${esc(state)}</span></div><div class="dir-vehicle-data"><div><span>Placas</span><b>${esc(v.placas||'—')}</b></div><div><span>Kilometraje</span><b>${Number(v.kilometraje||0).toLocaleString('es-MX')} km</b></div><div><span>Proyecto</span><b>${esc(v.proyecto||'Sin asignar')}</b></div><div><span>Responsable</span><b>${esc(v.responsable||v.asignadoA||'—')}</b></div></div><div class="dir-doc" data-tone="${docs.tone}">${esc(docs.label)}</div><details><summary>Documentación y datos</summary><div class="dir-details"><div><span>Seguro</span><b>${esc(v.aseguradora||'—')} · ${fmtDate(v.vigenciaSeguro)}</b></div><div><span>Tarjeta circulación</span><b>${fmtDate(v.vigenciaTarjeta)}</b></div><div><span>Próxima verificación</span><b>${fmtDate(v.proximaVerificacion)}</b></div><div><span>Combustible</span><b>${esc(v.combustible||'—')}</b></div><div><span>VIN</span><b>${esc(v.vin||'—')}</b></div><div><span>Notas</span><b>${esc(v.notas||'—')}</b></div></div></details></article>`}).join('')}
async function load(force=false){if(loadPromise&&!force)return loadPromise;const status=$('load-status'),reload=$('reload');const run=(async()=>{if(reload)reload.disabled=true;try{status.textContent='Consultando flotilla…';const task=window.SkilledDB.listExecutiveVehicles?window.SkilledDB.listExecutiveVehicles():window.SkilledDB.listVehicles();vehicles=await (window.SkilledDB.withOperationTimeout?window.SkilledDB.withOperationTimeout(task,16000,'La consulta de vehículos no respondió a tiempo.'):Promise.race([task,new Promise((_,reject)=>setTimeout(()=>reject(new Error('La consulta de vehículos no respondió a tiempo.')),16000))]));const states=[...new Set(vehicles.map(v=>text(v.estado)).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'es'));$('status').innerHTML='<option value="">Todos los estados</option>'+states.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');render();status.textContent=`Actualizado ${new Date().toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})}`}catch(error){const message=error.message||'No se pudo consultar la flotilla.';status.textContent='No disponible';$('grid').innerHTML=`<div class="dir-vehicle-empty error"><strong>No se pudo cargar la flotilla.</strong><br>${esc(message)}<br><br>Usa “Actualizar” para volver a intentarlo.</div>`}finally{if(reload)reload.disabled=false}})();loadPromise=run.finally(()=>{loadPromise=null});return loadPromise}
function boot(){ $('search')?.addEventListener('input',render);$('status')?.addEventListener('change',render);$('reload')?.addEventListener('click',()=>load(true));const start=()=>load(false);window.addEventListener('skilled:sessionready',start,{once:true});if(window.SkilledSession)queueMicrotask(start)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
