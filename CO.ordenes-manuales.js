(function(){
'use strict';
const $=id=>document.getElementById(id);const text=v=>String(v??'').trim();const num=v=>{const n=Number(v);return Number.isFinite(n)?n:0};const lower=v=>text(v).toLocaleLowerCase('es-MX');const esc=v=>text(v).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
let materials=[],warehouses=[],providers=[],projects=[],items=[];
function materialStock(material,warehouseId){const row=(material.almacenes||[]).find(x=>Number(x.id)===Number(warehouseId));return row?num(row.stock):0}
function materialInventory(material,warehouseId){return(material.almacenes||[]).find(x=>Number(x.id)===Number(warehouseId))||{}}
function openModal(){items=[];$('manual-order-number').value='';$('manual-order-reference').value='';$('manual-order-requested').value='';$('manual-order-justification').value='';$('manual-order-search').value='';$('manual-order-date').value='';$('manual-order-priority').value='normal';renderItems();renderSearch();$('manual-order-modal').classList.remove('hidden');$('manual-order-modal').classList.add('flex');setTimeout(()=>$('manual-order-search').focus(),50)}
function closeModal(){$('manual-order-modal').classList.add('hidden');$('manual-order-modal').classList.remove('flex')}
function renderSearch(){const q=lower($('manual-order-search').value);const warehouseId=Number($('manual-order-warehouse').value||0);const selected=new Set(items.map(x=>lower(x.materialCodigo)));const rows=materials.filter(m=>!selected.has(lower(m.codigo))&&(!q||(window.SkilledSearch?.matches?SkilledSearch.matches([m.codigo,m.codigoMarca,m.descripcion,m.marca,m.categoria,m.proveedor],q):lower([m.codigo,m.codigoMarca,m.descripcion,m.marca,m.categoria,m.proveedor].join(' ')).includes(q)))).slice(0,12);$('manual-order-results').innerHTML=rows.length?rows.map(m=>{const inv=materialInventory(m,warehouseId);return`<button type="button" data-manual-add="${esc(m.codigo)}" class="w-full rounded-xl border border-[#1d2949] bg-[#0b1222] p-3 text-left hover:border-blue-500/40"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold text-white">${esc(m.descripcion||m.desc)}</p><p class="mt-1 text-[9px] text-gray-500">${esc(m.codigo)}${m.codigoMarca?` · ${esc(m.codigoMarca)}`:''} · ${esc(m.marca||'Sin marca')}</p></div><span class="text-[9px] ${num(inv.stock)>0?'text-emerald-300':'text-gray-500'}">Stock ${num(inv.stock).toLocaleString('es-MX')}</span></div></button>`}).join(''):'<div class="rounded-xl border border-dashed border-[#243257] p-5 text-center text-xs text-gray-500">Escribe código, descripción, marca o modelo para agregar partidas.</div>';document.querySelectorAll('[data-manual-add]').forEach(b=>b.onclick=()=>addMaterial(b.dataset.manualAdd))}
function addMaterial(code){const m=materials.find(x=>lower(x.codigo)===lower(code));if(!m)return;items.push({materialCodigo:m.codigo,descripcion:m.descripcion||m.desc,categoria:m.categoria,unidad:m.unidad,marca:m.marca,cantidad:1,precioUnitario:num(m.precio),moneda:text(m.monedaCosto||m.moneda_costo||'MXN')||'MXN'});renderItems();renderSearch()}
function renderItems(){const warehouseId=Number($('manual-order-warehouse').value||0);$('manual-order-items').innerHTML=items.length?items.map((item,index)=>{const m=materials.find(x=>lower(x.codigo)===lower(item.materialCodigo))||{};return`<div class="grid grid-cols-1 lg:grid-cols-[1.8fr_.55fr_.7fr_.55fr_auto] gap-2 items-end rounded-xl border border-[#1d2949] bg-[#0b1222] p-3"><div><p class="text-xs font-bold text-white">${esc(item.descripcion)}</p><p class="mt-1 text-[9px] text-gray-500">${esc(item.materialCodigo)} · ${esc(item.marca||m.marca||'Sin marca')} · Stock destino ${materialStock(m,warehouseId).toLocaleString('es-MX')}</p></div><label class="crm-label">Cantidad<input data-manual-qty="${index}" type="number" min="0.01" step="0.01" class="field mt-1" value="${item.cantidad}"></label><label class="crm-label">Precio referencial<input data-manual-price="${index}" type="number" min="0" step="0.01" class="field mt-1" value="${item.precioUnitario||0}"></label><label class="crm-label">Moneda<select data-manual-currency="${index}" class="field mt-1"><option value="MXN" ${item.moneda==='MXN'?'selected':''}>MXN</option><option value="USD" ${item.moneda==='USD'?'selected':''}>USD</option><option value="EUR" ${item.moneda==='EUR'?'selected':''}>EUR</option></select></label><button type="button" data-manual-remove="${index}" class="crm-secondary text-rose-300">Quitar</button></div>`}).join(''):'<div class="rounded-xl border border-dashed border-[#243257] p-6 text-center text-xs text-gray-500">Todavía no agregas partidas a esta orden extraordinaria.</div>';$('manual-order-count').textContent=`${items.length} partida${items.length===1?'':'s'}`;document.querySelectorAll('[data-manual-qty]').forEach(el=>el.oninput=()=>items[Number(el.dataset.manualQty)].cantidad=Math.max(num(el.value),0));document.querySelectorAll('[data-manual-price]').forEach(el=>el.oninput=()=>items[Number(el.dataset.manualPrice)].precioUnitario=Math.max(num(el.value),0));document.querySelectorAll('[data-manual-currency]').forEach(el=>el.onchange=()=>items[Number(el.dataset.manualCurrency)].moneda=el.value);document.querySelectorAll('[data-manual-remove]').forEach(el=>el.onclick=()=>{items.splice(Number(el.dataset.manualRemove),1);renderItems();renderSearch()})}
async function save(){const warehouseId=Number($('manual-order-warehouse').value||0);const justification=text($('manual-order-justification').value);if(!warehouseId)return alert('Selecciona el almacén destino.');if(!justification)return alert('Escribe la justificación de la compra extraordinaria.');if(!items.length)return alert('Agrega al menos una partida.');if(items.some(x=>num(x.cantidad)<=0))return alert('Todas las cantidades deben ser mayores a cero.');const b=$('manual-order-save');b.disabled=true;b.textContent='Creando orden…';try{const result=await SkilledDB.createManualPurchaseOrderV73({ordenCompra:$('manual-order-number').value,proveedorId:Number($('manual-order-provider').value||0)||null,almacenId:warehouseId,proyecto:$('manual-order-project').value,prioridad:$('manual-order-priority').value,fechaRequerida:$('manual-order-date').value,referencia:$('manual-order-reference').value,solicitadoPor:$('manual-order-requested').value,justificacion,items});closeModal();alert(`Orden extraordinaria ${result.orden||''} creada con ${result.materiales||items.length} partida(s).`);if(typeof window.load==='function')await window.load();else location.reload()}catch(e){alert(e.message)}finally{b.disabled=false;b.textContent='Crear orden extraordinaria'}}
async function init(){try{[materials,warehouses,projects]=await Promise.all([SkilledDB.listMaterials(),SkilledDB.listWarehouses(),SkilledDB.listProjects().catch(()=>[])]);const out=await SkilledDB.client.from('co_proveedores').select('id,razon_social,nombre_comercial,contacto,estado').neq('estado','inactivo').order('razon_social',{ascending:true});if(!out.error)providers=out.data||[];$('manual-order-warehouse').innerHTML='<option value="">Selecciona almacén…</option>'+warehouses.map(x=>`<option value="${x.id}">${esc(x.nombre)}</option>`).join('');$('manual-order-provider').innerHTML='<option value="">Proveedor por definir</option>'+providers.map(x=>`<option value="${x.id}">${esc(x.nombre_comercial||x.razon_social)}</option>`).join('');$('manual-order-project').innerHTML='<option value="">Sin proyecto</option>'+projects.map(x=>`<option value="${esc(x.numeroProyecto||x.proyecto||x.numero_proyecto)}">${esc(x.numeroProyecto||x.proyecto||x.numero_proyecto)} · ${esc(x.nombreProyecto||x.nombre_proyecto||'')}</option>`).join('')}catch(e){console.error(e)}}
document.addEventListener('DOMContentLoaded',()=>{const open=$('manual-order-open');if(!open)return;open.addEventListener('click',openModal);$('manual-order-close').addEventListener('click',closeModal);$('manual-order-cancel').addEventListener('click',closeModal);$('manual-order-save').addEventListener('click',save);$('manual-order-search').addEventListener('input',renderSearch);$('manual-order-warehouse').addEventListener('change',()=>{renderSearch();renderItems()});$('manual-order-modal').addEventListener('click',e=>{if(e.target===$('manual-order-modal'))closeModal()});init()})
})();
