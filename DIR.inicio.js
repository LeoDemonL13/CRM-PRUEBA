(function(){
'use strict';
let loadPromise=null;
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'),num=v=>Number(v)||0;
const money=v=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN',maximumFractionDigits:0}).format(num(v));
const date=v=>{if(!v)return'—';const d=new Date(`${v}T12:00:00`);return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString('es-MX',{day:'2-digit',month:'short',year:'numeric'})};
function role(){return String(window.SkilledSession?.role||document.body.dataset.profile||'').toLowerCase()}
function prefix(){return role()==='gerente_general'?'GG':'SG'}
function isClosed(row){return /complet|cerrad|cancelad/i.test(String(row.estado||''))}
function daysTo(row){if(!row.fechaEntrega)return null;const target=new Date(`${row.fechaEntrega}T12:00:00`),today=new Date();today.setHours(0,0,0,0);if(Number.isNaN(target.getTime()))return null;return Math.ceil((target-today)/86400000)}
function utilization(row){const plan=num(row.total_planeado),real=num(row.total_real);return plan>0?real/plan*100:real>0?100:0}
function risk(row){const days=daysTo(row),plan=num(row.total_planeado),dev=num(row.desviacion_total),use=utilization(row);let score=0;if(dev>0)score+=25+Math.min(55,plan>0?dev/plan*100:35);if(use>=100)score+=22;else if(use>=85)score+=10;if(days!==null&&days<0)score+=60;else if(days!==null&&days<=7)score+=38;else if(days!==null&&days<=14)score+=20;if(!String(row.responsable||'').trim())score+=8;return score}
function riskMeta(row){const score=risk(row),days=daysTo(row),dev=num(row.desviacion_total);if(score>=70)return{label:'Crítico',cls:'critical'};if(score>=35)return{label:'Atención',cls:'warning'};if(dev>0)return{label:'Sobre plan',cls:'warning'};if(days!==null&&days<=14)return{label:'Entrega próxima',cls:'info'};return{label:'Estable',cls:'ok'}}
function deliveryText(days){if(days===null)return'Sin fecha';if(days<0)return`${Math.abs(days)} días vencido`;if(days===0)return'Vence hoy';if(days===1)return'Vence mañana';return`Faltan ${days} días`}
function setText(id,value){const node=$(id);if(node)node.textContent=value}
function snapshotKey(){return`skilled_exec_snapshot_v82_${role()||'direccion'}`}
function readSnapshot(){try{return JSON.parse(localStorage.getItem(snapshotKey())||'null')}catch(_){return null}}
function saveSnapshot(value){try{localStorage.setItem(snapshotKey(),JSON.stringify(value))}catch(_){}}
function renderOperationalAlerts(alerts){const summary=alerts?.summary||{},values={low:num(summary.bajoMinimo),purchases:num(summary.comprasPendientes),locations:num(summary.ubicacionesPendientes),tools:num(summary.herramientasVencidas),vehicles:num(summary.documentosVehiculo)};setText('ops-low',values.low.toLocaleString('es-MX'));setText('ops-purchases',values.purchases.toLocaleString('es-MX'));setText('ops-locations',values.locations.toLocaleString('es-MX'));setText('ops-tools',values.tools.toLocaleString('es-MX'));setText('ops-vehicles',values.vehicles.toLocaleString('es-MX'));[['alert-low',values.low],['alert-purchases',values.purchases],['alert-locations',values.locations],['alert-tools',values.tools],['alert-vehicles',values.vehicles]].forEach(([id,value])=>{const node=$(id);if(!node)return;node.dataset.alert=value>0?'1':'0';node.dataset.critical=value>=5?'1':'0';node.hidden=value===0});const total=Object.values(values).reduce((a,b)=>a+b,0);setText('ops-status',total?`${total} señales`:'Sin alertas');const section=$('ops-status')?.closest('.exec-section');if(section)section.hidden=total===0;return{...values,total}}
function ensureExecutiveEnhancements(){if(document.getElementById('exec-decision-center'))return;const main=document.querySelector('main.profile-shell')||document.querySelector('main'),metrics=document.querySelector('.exec-metrics');if(!main||!metrics)return;const section=document.createElement('section');section.id='exec-decision-center';section.className='exec-section exec-decision-center';section.innerHTML=`<div class="exec-section-head"><div><h2>Resumen de decisión</h2><p>Solo concentra lo que puede requerir una decisión o seguimiento; el detalle operativo permanece en cada área.</p></div><div class="flex gap-2"><button id="exec-validate-oc" class="crm-secondary" type="button">Validar OC</button><button id="exec-ask-sky" class="crm-primary" type="button">Preguntar a Skill</button><button id="exec-copy-brief" class="crm-secondary" type="button">Copiar resumen</button></div></div><div class="exec-decision-grid"><article class="exec-panel"><header class="exec-panel-head"><div><h2 id="decision-title">Estado ejecutivo</h2><p id="decision-copy">Analizando operación…</p></div><span id="decision-status" class="profile-badge">Calculando</span></header><div class="exec-panel-body"><div id="decision-cards" class="exec-decision-cards"></div></div></article><article class="exec-panel"><header class="exec-panel-head"><div><h2>Cambios desde la última revisión</h2><p>Comparación local del tablero para evitar volver a revisar lo que no cambió.</p></div></header><div class="exec-panel-body"><div id="exec-change-list" class="exec-change-list"><div class="exec-empty">Preparando comparación…</div></div><div id="exec-sky-prompts" class="exec-sky-prompts-inline"></div></div></article></div>`;metrics.insertAdjacentElement('afterend',section);document.getElementById('exec-copy-brief')?.addEventListener('click',async()=>{const value=document.getElementById('decision-copy')?.textContent||'';try{await navigator.clipboard.writeText(value);document.getElementById('exec-copy-brief').textContent='Copiado'}catch(_){document.getElementById('exec-copy-brief').textContent='No copió'}setTimeout(()=>{const b=document.getElementById('exec-copy-brief');if(b)b.textContent='Copiar resumen'},1400)});document.getElementById('exec-ask-sky')?.addEventListener('click',()=>askSky('Skill, dame un resumen ejecutivo de lo que requiere atención hoy'));document.getElementById('exec-validate-oc')?.addEventListener('click',()=>{location.href=`${prefix()}.validar-ordenes-compra.html`})}
function askSky(q){if(window.SkilledSky){window.SkilledSky.open();setTimeout(()=>window.SkilledSky.query(q),100);return}document.getElementById('sky-open')?.click();setTimeout(()=>window.SkilledSky?.query(q),420)}
function deltaText(label,current,previous,formatter=v=>String(v)){const diff=num(current)-num(previous);if(!diff)return'';const sign=diff>0?'+':'';return`${label}: ${sign}${formatter(diff)}`}
function renderChanges(snapshot){const host=$('exec-change-list');if(!host)return;const prev=readSnapshot();if(!prev){host.innerHTML='<div class="exec-change-item good"><strong>Primera revisión registrada</strong><span>La próxima vez Skill y el tablero podrán mostrar qué cambió desde esta lectura.</span></div>';saveSnapshot(snapshot);return}const changes=[deltaText('Gasto real',snapshot.real,prev.real,money),deltaText('Proyectos críticos',snapshot.critical,prev.critical),deltaText('Entregas próximas',snapshot.due,prev.due),deltaText('Señales operativas',snapshot.alertTotal,prev.alertTotal)].filter(Boolean);host.innerHTML=changes.length?changes.map(value=>`<div class="exec-change-item"><strong>${esc(value)}</strong><span>Variación desde la última vez que se abrió este tablero en este equipo.</span></div>`).join(''):'<div class="exec-change-item good"><strong>Sin cambios relevantes</strong><span>Los indicadores principales se mantienen iguales a la revisión anterior.</span></div>';saveSnapshot(snapshot)}
function renderDecisionCenter(rows=[],alerts={}){ensureExecutiveEnhancements();const active=rows.filter(row=>!isClosed(row)),planned=rows.reduce((s,r)=>s+num(r.total_planeado),0),real=rows.reduce((s,r)=>s+num(r.total_real),0),over=active.filter(row=>num(row.desviacion_total)>0),due=active.filter(row=>{const d=daysTo(row);return d!==null&&d<=14}),critical=active.filter(row=>risk(row)>=70),warning=active.filter(row=>risk(row)>=35&&risk(row)<70),ops=renderOperationalAlerts(alerts);const budgetPct=planned>0?real/planned*100:real>0?100:0,level=critical.length||budgetPct>100||ops.total>=8?'Atención alta':warning.length||due.length||ops.total?'Vigilancia':'Estable';const copy=role()==='gerente_general'?`${active.length} proyectos activos. El portafolio utiliza ${Math.round(budgetPct)}% de lo planeado; ${critical.length} proyectos están en nivel crítico, ${due.length} tienen entrega próxima o vencida y ${ops.total} señales operativas requieren seguimiento.`:`${active.length} proyectos activos. ${critical.length+warning.length} requieren seguimiento, ${due.length} tienen entrega próxima o vencida y existen ${ops.total} señales operativas abiertas que podrían afectar la ejecución.`;setText('decision-status',level);setText('decision-copy',copy);setText('decision-title',role()==='gerente_general'?'Lectura para decisión':'Lectura para seguimiento');const cards=[{t:'Uso de presupuesto',v:`${Math.round(budgetPct)}%`,d:`${money(real)} real · ${money(planned)} planeado`,c:budgetPct>100},{t:'Proyectos con atención',v:String(critical.length+warning.length),d:`${critical.length} críticos · ${warning.length} en vigilancia`,c:critical.length>0},{t:'Entregas ≤ 14 días',v:String(due.length),d:'Incluye vencidos y próximos',c:due.length>0}];if(ops.total>0)cards.push({t:'Señales operativas',v:String(ops.total),d:`Mínimos ${ops.low} · compras ${ops.purchases} · ubicaciones ${ops.locations}`,c:ops.total>=5});const cardHost=$('decision-cards');if(cardHost)cardHost.innerHTML=cards.map(item=>`<article class="exec-decision-card" data-critical="${item.c?'1':'0'}"><span>${esc(item.t)}</span><b>${esc(item.v)}</b><small>${esc(item.d)}</small></article>`).join('');const prompts=role()==='gerente_general'?['Skill, ¿qué requiere una decisión hoy?','Skill, ¿qué proyectos se alejaron más de lo planeado?','Skill, resúmeme las próximas entregas']:['Skill, ¿qué debo revisar primero hoy?','Skill, ¿qué proyectos están en riesgo?','Skill, ¿qué pendientes pueden afectar una entrega?'];const promptHost=$('exec-sky-prompts');if(promptHost)promptHost.innerHTML=prompts.map(q=>`<button class="exec-sky-prompt" type="button" data-exec-sky="${esc(q)}">${esc(q)}</button>`).join('');promptHost?.querySelectorAll('[data-exec-sky]').forEach(button=>button.addEventListener('click',()=>askSky(button.dataset.execSky||'')));renderChanges({real,critical:critical.length,due:due.length,alertTotal:ops.total,at:Date.now()})}
function injectExecutiveStyles(){if(document.getElementById('exec-v82-styles'))return;const style=document.createElement('style');style.id='exec-v82-styles';style.textContent=`.exec-decision-grid{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(300px,.85fr);gap:14px}.exec-decision-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));gap:10px}.exec-decision-card{border:1px solid #1a2946;border-radius:12px;background:#0b1426;padding:12px;min-height:82px}.exec-decision-card span{display:block;color:#6d7d95;font-size:8px;font-weight:850;text-transform:uppercase;letter-spacing:.1em}.exec-decision-card b{display:block;margin-top:8px;color:#f8fafc;font-size:21px;font-weight:900}.exec-decision-card small{display:block;margin-top:4px;color:#74839a;font-size:8px;line-height:1.4}.exec-decision-card[data-critical="1"]{border-color:rgba(245,158,11,.32);background:rgba(92,57,9,.08)}.exec-change-list{display:flex;flex-direction:column;gap:8px}.exec-change-item{border:1px solid #1c2d4b;border-radius:10px;background:#0a1323;padding:9px 10px}.exec-change-item strong{display:block;color:#dbeafe;font-size:9px}.exec-change-item span{display:block;margin-top:3px;color:#6f8098;font-size:8px;line-height:1.4}.exec-change-item.good{border-color:rgba(52,211,153,.22);background:rgba(6,78,59,.07)}.exec-sky-prompts-inline{display:flex;flex-wrap:wrap;gap:7px;margin-top:11px}.exec-sky-prompt{border:1px solid #1d3357;border-radius:999px;background:#0b1426;color:#b8d8ff;padding:7px 9px;font-size:8px;font-weight:750}.exec-sky-prompt:hover{border-color:#3b82f6;background:#10203b}#exec-validate-oc.has-pending{border-color:rgba(245,158,11,.5)!important;color:#fcd34d!important;background:rgba(146,64,14,.12)!important}.exec-alert-card[hidden],.exec-section[hidden]{display:none!important}.tema-claro .exec-decision-card,.tema-claro .exec-change-item,.tema-claro .exec-sky-prompt{background:#fff!important;border-color:#dbe3ef!important}.tema-claro .exec-decision-card b{color:#0f172a!important}.tema-claro .exec-change-item strong{color:#1e3a8a!important}@media(max-width:1100px){.exec-decision-grid{grid-template-columns:1fr}}`;document.head.appendChild(style)}
function card(row,mode='risk'){const meta=riskMeta(row),days=daysTo(row),use=Math.round(utilization(row)),dev=num(row.desviacion_total);return`<a class="exec-priority" href="${prefix()}.proyectos.html?proyecto=${encodeURIComponent(row.proyecto)}"><div class="exec-priority-main"><div class="exec-project-line"><span class="exec-code">${esc(row.proyecto)}</span><span class="exec-risk ${meta.cls}">${meta.label}</span></div><strong>${esc(row.nombre||'Proyecto')}</strong><small>${esc(row.cliente||'Sin cliente')} · ${esc(row.responsable||'Responsable pendiente')}</small></div><div class="exec-priority-side"><b>${mode==='delivery'?date(row.fechaEntrega):money(row.total_real)}</b><span>${mode==='delivery'?deliveryText(days):`${use}% utilizado${dev>0?` · +${money(dev)}`:''}`}</span></div></a>`}
function render(rows){const active=rows.filter(row=>!isClosed(row)),planned=rows.reduce((s,r)=>s+num(r.total_planeado),0),real=rows.reduce((s,r)=>s+num(r.total_real),0),dev=real-planned,over=active.filter(r=>num(r.desviacion_total)>0),due=active.filter(r=>{const d=daysTo(r);return d!==null&&d<=14}),mat=rows.reduce((s,r)=>s+num(r.material_real),0),pay=rows.reduce((s,r)=>s+num(r.nomina_real),0),pct=planned>0?real/planned*100:real>0?100:0;setText('metric-active',active.length.toLocaleString('es-MX'));setText('metric-planned',money(planned));setText('metric-real',money(real));setText('metric-deviation',`${dev>0?'+':''}${money(dev)}`);setText('metric-over',over.length.toLocaleString('es-MX'));setText('metric-due',due.length.toLocaleString('es-MX'));const devNode=$('metric-deviation');if(devNode)devNode.className=`metric-value ${dev>0?'text-rose-300':dev<0?'text-emerald-300':'text-white'}`;setText('budget-percent',`${Math.round(pct)}%`);setText('budget-detail',`${money(real)} utilizados de ${money(planned)} planeados`);if($('budget-bar'))$('budget-bar').style.width=`${Math.min(100,Math.max(0,pct))}%`;if($('budget-bar'))$('budget-bar').classList.toggle('over',pct>100);const totalMix=mat+pay,matPct=totalMix>0?mat/totalMix*100:0,payPct=totalMix>0?pay/totalMix*100:0;setText('mix-material',`${money(mat)} · ${Math.round(matPct)}%`);setText('mix-payroll',`${money(pay)} · ${Math.round(payPct)}%`);if($('mix-material-bar'))$('mix-material-bar').style.width=`${matPct}%`;if($('mix-payroll-bar'))$('mix-payroll-bar').style.width=`${payPct}%`;const attention=active.map(row=>({row,score:risk(row)})).filter(item=>item.score>=20).sort((a,b)=>b.score-a.score||num(b.row.desviacion_total)-num(a.row.desviacion_total)).slice(0,5).map(item=>item.row);$('attention-list').innerHTML=attention.length?attention.map(row=>card(row,'risk')).join(''):'<div class="exec-empty">No hay proyectos con señales de atención inmediata.</div>';const deliveries=active.map(row=>({row,days:daysTo(row)})).filter(item=>item.days!==null&&item.days<=30).sort((a,b)=>a.days-b.days).slice(0,5).map(item=>item.row);$('delivery-list').innerHTML=deliveries.length?deliveries.map(row=>card(row,'delivery')).join(''):'<div class="exec-empty">No hay entregas dentro de los próximos 30 días.</div>';const focus=role()==='gerente_general'?[...rows].filter(r=>num(r.desviacion_total)>0).sort((a,b)=>num(b.desviacion_total)-num(a.desviacion_total)).slice(0,5):[...active].filter(r=>risk(r)>0).sort((a,b)=>risk(b)-risk(a)).slice(0,5);$('project-list').innerHTML=focus.length?focus.map(row=>card(row,'risk')).join(''):'<div class="exec-empty">No hay excepciones relevantes para mostrar.</div>';setText('focus-title',role()==='gerente_general'?'Excepciones presupuestales':'Seguimiento prioritario');setText('focus-copy',role()==='gerente_general'?'Solo proyectos con desviación; el resto permanece fuera de esta vista para reducir ruido.':'Solo proyectos con señales de riesgo, fecha o utilización que requieren seguimiento.');setText('last-update',`Actualizado ${new Date().toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})}`);const healthy=active.filter(row=>risk(row)<35).length;setText('health-summary',active.length?`${healthy} de ${active.length} proyectos activos se mantienen sin señales críticas.`:'No hay proyectos activos.')}
function bounded(value,ms,message){return window.SkilledDB?.withOperationTimeout?window.SkilledDB.withOperationTimeout(value,ms,message):Promise.race([Promise.resolve(value),new Promise((_,reject)=>setTimeout(()=>reject(new Error(message)),ms))])}
async function refreshPendingOCValidation(){const button=$('exec-validate-oc');if(!button||typeof SkilledDB?.listExecutivePendingPurchaseOrdersV136!=='function')return;try{const pending=await bounded(SkilledDB.listExecutivePendingPurchaseOrdersV136(),7000,'');const count=Array.isArray(pending)?pending.length:0;button.textContent=count?`Validar OC · ${count}`:'Validar OC';button.classList.toggle('has-pending',count>0)}catch(_){button.textContent='Validar OC'}}
async function load(){if(loadPromise)return loadPromise;const run=(async()=>{try{injectExecutiveStyles();ensureExecutiveEnhancements();refreshPendingOCValidation();setText('last-update','Actualizando…');const projects=await bounded(SkilledDB.getExecutiveProjectSummary(),15000,'El resumen de proyectos no respondió a tiempo.');render(projects||[]);setText('last-update',`Actualizado ${new Date().toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})}`);let alerts={};try{if(SkilledDB.listOperationalAlerts)alerts=await bounded(SkilledDB.listOperationalAlerts(),10000,'Las alertas operativas tardaron demasiado.')}catch(error){console.warn('Alertas operativas no disponibles:',error);setText('ops-status','No disponible')}renderDecisionCenter(projects||[],alerts||{})}catch(e){if($('attention-list'))$('attention-list').innerHTML=`<div class="exec-error"><strong>No se pudo cargar el tablero.</strong><br>${esc(e.message||'La consulta no respondió.')}<br><br>Actualiza la página para volver a intentarlo.</div>`;if($('delivery-list'))$('delivery-list').innerHTML='';if($('project-list'))$('project-list').innerHTML='';setText('last-update','No se pudo actualizar');setText('ops-status','No disponible')}})();loadPromise=run.finally(()=>{loadPromise=null});return loadPromise}
window.addEventListener('skilled:sessionready',load,{once:true});if(window.SkilledSession)queueMicrotask(load);
})();
